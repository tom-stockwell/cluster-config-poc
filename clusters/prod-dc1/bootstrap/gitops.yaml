apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gitops
  labels:
    app: gitops
spec:
  project: default
  source:
    repoURL: https://github.com/tom-stockwell/cluster-config-poc.git
    targetRevision: HEAD
    path: clusters/prod-dc1

  destination:
    name: in-cluster
